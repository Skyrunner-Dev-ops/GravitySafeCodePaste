# Gravity assets

Place the Gravity mascot/logo artwork in this folder as `gravity-mascot.png` when updating the repository from Git or the GitHub web UI.

The static `index.html` falls back gracefully when the image is not present.